{{!-- <tr class="table-row">
    <td class="table-first-col">{{id}} {{burger_name}}</td>
    <td><button class="change-devour" data-id="{{id}}">Devour it!</button></td> 
</tr> --}}

<li>
	{{name}}

{{#unless devoured}}
	<button class="devour" data-id="{{id}}" data-newdev="{{devoured}}">
		Eat it!!
	</button>
{{/unless}}

</li>