<tr class="table-row">
	<li>
    <td class="table-first-col">{{burger_name}}</td><br>
    <td><button class="change-devour btn btn-secondary btn-sm" type="button" data-id="{{id}}">
		{{#if devoured}}Devoured{{else}}Devour It!{{/if}}
	</button></td>
	<button class="delete-burger btn btn-secondary btn-sm" data-id="{{this.id}}">Delete</button>
	</li> 
</tr>